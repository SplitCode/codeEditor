{
  "name": "json-server-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start:server": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}